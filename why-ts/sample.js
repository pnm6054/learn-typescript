// function sum(a, b) {
//     return a + b;
// }
// // sum(10, '20'); // 1020
// var result1 = sum(10, 20);
// result.toLocaleString();

// @ts-check <-- 이거 있으면 js 파일에서 ts 처럼 검사

/**
 * 
 * @param {number} a 첫번째 숫자
 * @param {number} b 두번째 숫자
 */
function sum(a, b) {
    return a + b;
}

// sum(10, '20'); // 이거 잘못됐다고 알려주는건 ts만 가능